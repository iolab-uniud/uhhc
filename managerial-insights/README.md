## Usage

This analysis requires running all three scripts in the directory to generate plots for managerial insights. 
To reproduce the environment, use `uv sync` to install the necessary dependencies as specified in the project configuration.

